{
  "name": "lyricat",
  "private": true,
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "tauri": "tauri",
    "build": "vite build",
    "sync:version": "node scripts/sync-tauri-version.mjs",
    "version:patch": "npm version patch --no-git-tag-version && npm run sync:version",
    "version:minor": "npm version minor --no-git-tag-version && npm run sync:version",
    "version:major": "npm version major --no-git-tag-version && npm run sync:version",
    "build:msi": "npm run sync:version && tauri build --bundles msi",
    "build:msi:named": "npm run build:msi && node scripts/rename-msi.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@tauri-apps/plugin-opener": "^2.5.3",
    "@tauri-apps/plugin-shell": "^2.3.5",
    "tailwindcss": "^4.2.1"
  }
}
